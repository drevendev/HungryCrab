## Summary

<!-- What changed and why. Name the issue it closes: "Closes #NN". -->

## Tests

<!-- What proves it: the new or changed test cases, and the count that passes now. -->

- [ ] `uv run pytest` passes
- [ ] `uv run ruff check . && uv run ruff format --check . && uv run mypy` pass
- [ ] `CHANGELOG.md` has an entry under `[Unreleased]`, or the change is not user-visible
- [ ] Nothing here executes prey content, and prey text is treated as untrusted data
