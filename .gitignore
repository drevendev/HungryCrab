# Python
__pycache__/
*.py[cod]
*.egg-info/
/build/
/dist/
.venv/
venv/

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Editors and OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Local agent settings
.claude/settings.local.json

# Local digest output (the cache lives under ~/.cache/hungry-crab)
digest-out/

# The prey backlog and the researcher prompt name private repositories and planned targets;
# this repository is public. They now live, with history, in a private companion repository —
# see docs/design/03-roadmap.md on why the queue is not part of this repository. The entries stay
# so that a stray local copy cannot be committed by accident.
.crab/prey-backlog.md
.crab/researcher-prompt.md
