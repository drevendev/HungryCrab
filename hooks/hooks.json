{
  "description": "Never execute prey. Blocks a Bash command that would run code out of the prey cache.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "crab guard --hook"
          }
        ]
      }
    ]
  }
}
