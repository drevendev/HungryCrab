"""Hungry Crab: eat a foreign repository, digest it, and extract what is useful for your own.

The package is a deterministic command-line tool. It never executes code from the repositories
it analyses and never calls a language model; judgment is left to the Agent Skills built on top.
"""

from __future__ import annotations

__version__ = "0.2.0"
